# Tech debt cleanup workflow
# Focuses on cleaning up technical debt in the codebase
commands:
    - name: mmm-cleanup-tech-debt
      id: cleanup
      outputs:
        spec:
          file_pattern: "specs/temp/*-tech-debt-cleanup.md"
    
    - name: mmm-implement-spec
      inputs:
        spec:
          from: "${cleanup.spec}"
          pass_as:
            argument:
              position: 0
    
    - name: mmm-lint