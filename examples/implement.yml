# implement.yml - Example configuration for implementing specifications
# This replaces the old `mmm implement` functionality
#
# Usage examples:
#   mmm improve --config examples/implement.yml --args "33"
#   mmm improve --config examples/implement.yml --map "specs/*.md"
#   mmm improve --config examples/implement.yml --map "specs/temp/*.md" --worktree

workflow:
  # Commands to execute for each specification
  commands:
    # Implement the spec using the $ARG variable which contains:
    # - The spec ID extracted from filename when using --map
    # - The direct argument value when using --args
    - name: mmm-implement-spec
      args: ["$ARG"]
    
    # Run linting and formatting after implementation
    - name: mmm-lint
  

# Alternative configuration with structured commands:
# workflow:
#   commands:
#     - name: mmm-implement-spec
#       args: ["$ARG"]
#       metadata:
#         retries: 3
#         timeout: 600
#     - name: mmm-lint
#       metadata:
#         continue_on_error: true
